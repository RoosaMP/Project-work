<h1>{{pagetitle}}</h1>


<div class="productcard">
    {{#each products}}
        <div class="">
            {{#if this.img.data}}
            <p><img src="data:{{this.img.contentType}};base64,{{base64 this.img.data}}" width="50%"/></p>
            {{/if}}
            <p>Tuote: {{this.product}}</p>
            <p>Hinta: {{this.price}}€</p>
            <p>Tuotekuvaus: {{this.description}}</p>
            <p>Koko: {{this.size}}</p>
        </div>
    {{/each}}
</div>  